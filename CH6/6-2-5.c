#include <stdio.h>

int main(void) {
	int i, sum = 0;

	for (i = 1; i <= 10; i++) {
		if (i % 2 == 0) printf("i 출력값: %d\n", i);
		/*
		sum += i;
		if (sum > 30) break;
		*/
	}

	//printf("누적한 값 : %d\n", sum); 
	//printf("마지막으로 더한 값 : %d\n", i);




}